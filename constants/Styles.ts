import { ViewStyle, StyleSheet } from "react-native";

export default StyleSheet.create({
  generalContainer: {
    flexDirection: "column",
    alignItems: "center"
  } as ViewStyle,
  mainViewContainer: {
    flex: 1
  } as ViewStyle
});
